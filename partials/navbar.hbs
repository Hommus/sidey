<header aria-hidden="true" class="no-print">

   {{! check if a menu is assigned to the Main menu position }}
   {{#if menus.mainMenu}}
      {{> menu menus.mainMenu}}
   {{/if}}
   {{! /check if a menu is assigned to the Main menu position }}

</header>